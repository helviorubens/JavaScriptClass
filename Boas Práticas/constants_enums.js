/** @module Employee */

/** Constants and Enums */

const LIST_API_URL = "employee/"


/**
 * @enum
 * Posible address types.
 */
let AddressType = {
    /** Signifies the address type us a house. */
    House: "House",
    /** Signifies the address type us an apartment. */
    Apartment: "Apartment",
    /** Signifies the address type us a suite. */
    Suite: "Suite"
}